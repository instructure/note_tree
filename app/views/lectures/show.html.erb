<p id="notice"><%= notice %></p>

<p>
  <strong>Course:</strong>
  <%= @lecture.course.name %>
</p>

<p>
  <strong>Date:</strong>
  <%= @lecture.date %>
</p>

<p>
  <strong>Title:</strong>
  <%= @lecture.title %>
</p>

<p>
  <strong>Summary:</strong>
  <%= @lecture.summary %>
</p>

<% if current_account.teacher? %>
	<%= link_to 'Edit', edit_course_lecture_path(@course, @lecture) %> |
<% end %>
<%= link_to 'Back', course_lectures_path(@course) %>
<%= link_to 'Add New Notebook', {:controller => "notebooks", :action => "new", :course_id => @course.id} %> |
